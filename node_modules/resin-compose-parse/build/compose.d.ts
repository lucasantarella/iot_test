import { Composition, ImageDescriptor } from './types';
export declare function defaultComposition(image?: string): string;
export declare function normalize(c: any): Composition;
export declare function parse(c: Composition): ImageDescriptor[];
