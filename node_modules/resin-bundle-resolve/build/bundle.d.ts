/// <reference types="node" />
import * as Promise from 'bluebird';
export default class Bundle {
    tarStream: NodeJS.ReadableStream;
    deviceType: string;
    architecture: string;
    private dockerfileHook;
    constructor(tarStream: NodeJS.ReadableStream, deviceType: string, architecture: string, hook?: (content: string) => Promise<void>);
    callDockerfileHook(contents: string): Promise<void>;
}
