import * as Promise from 'bluebird';
import { FileInfo, Resolver } from '../resolver';
export default class DockerfileResolver implements Resolver {
    priority: number;
    name: string;
    private gotDockerfile;
    private dockerfileContents;
    entry(file: FileInfo): void;
    isSatisfied(): boolean;
    resolve(): Promise<FileInfo[]>;
    getDockerfileContents(): string;
}
