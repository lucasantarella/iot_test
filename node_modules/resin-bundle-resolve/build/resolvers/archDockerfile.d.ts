import * as Promise from 'bluebird';
import { Bundle, FileInfo, Resolver } from '../resolver';
export default class ArchDockerfileResolver implements Resolver {
    priority: number;
    name: string;
    private archDockerfiles;
    private satisifiedArch;
    private satisfiedDeviceType;
    entry(file: FileInfo): void;
    isSatisfied(bundle: Bundle): boolean;
    resolve(bundle: Bundle): Promise<FileInfo[]>;
}
