"use strict";
// copied from the @resin-io/device-types
// because that has the run-time dependency on coffee-script
Object.defineProperty(exports, "__esModule", { value: true });
var find = function (array, predicate) {
    for (var _i = 0, array_1 = array; _i < array_1.length; _i++) {
        var el = array_1[_i];
        if (predicate(el)) {
            return el;
        }
    }
};
var includes = function (array, el) { return array && array.indexOf(el) >= 0; };
var dtPredicate = function (slug) { return function (deviceType) {
    return deviceType.slug === slug || includes(deviceType.aliases, slug);
}; };
exports.findBySlug = function (deviceTypes, slug) {
    return find(deviceTypes, dtPredicate(slug));
};
exports.normalizeDeviceType = function (deviceTypes, slug) {
    // returns `undefined` in case of invalid slug
    var deviceType = exports.findBySlug(deviceTypes, slug);
    if (deviceType) {
        return deviceType.slug;
    }
};
